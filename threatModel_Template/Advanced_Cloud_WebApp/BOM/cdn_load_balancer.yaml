# BOM: CDN Load Balancer
asset: "CDN Load Balancer"
software_version: "Nginx 1.24.0 (reverse proxy / load balancer)"
patch_level: outdated
known_cves:
  - CVE-2023-44487  # HTTP/2 Rapid Reset Attack (DoS via request cancellation flood)
running_services:
  - HTTPS (443)
  - HTTP (80) — 301 redirect to HTTPS
  - Nginx status endpoint (127.0.0.1:8080)
detection_level: high
credentials_stored: false
notes: >
  Internet-facing reverse proxy and load balancer. CVE-2023-44487 (HTTP/2 Rapid Reset)
  requires Nginx 1.25.3+ or the ngx_http_v2_module patch. AWS WAF and rate limiting are
  configured upstream at the CDN layer. Access logs are shipped to a centralized SIEM
  (detection: high). TLS 1.2/1.3 only; TLS 1.0/1.1 disabled. HSTS header is set.
  Upgrade to Nginx 1.25.4+ to address the HTTP/2 DoS vulnerability.
