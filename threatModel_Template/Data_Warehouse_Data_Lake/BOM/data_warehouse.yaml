# BOM: DataWarehouse
asset: "DataWarehouse"
os_version: "managed_service"
software_version: "Snowflake (managed) or BigQuery (managed)"
patch_level: current
known_cves: []
running_services:
  - HTTPS (Snowflake/BigQuery API)
  - JDBC/ODBC
detection_level: high
credentials_stored: true
notes: "Cloud data warehouse (Snowflake or BigQuery). Service account credentials for ETL pipelines and BI tools stored in Airflow Variables and BI tool connections. Multi-factor authentication not enforced for analyst accounts. Snowflake network policies may not restrict access to corporate IP ranges. Query result caching may expose sensitive data to subsequent users on shared warehouses. SOX-critical financial aggregations stored here."
