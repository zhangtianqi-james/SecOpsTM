# BOM: ArtifactRegistry
asset: "ArtifactRegistry"
software_version: "JFrog Artifactory OSS 7.55.2"
patch_level: outdated
known_cves: []
running_services:
  - HTTPS (8443)
  - Docker Registry API (5000)
  - Maven/npm/PyPI repository APIs
detection_level: low
credentials_stored: false
notes: >
  Stores Docker images, Maven JARs, npm packages, and Python wheels produced by the CI
  pipeline. No known CVEs at this version but anonymous pull access is enabled by default
  in OSS edition — verify that push/delete requires authentication. Image signing
  (Notary/Cosign) is not configured. Upgrade to Artifactory 7.71+ recommended.
  No audit log forwarding to SIEM.
