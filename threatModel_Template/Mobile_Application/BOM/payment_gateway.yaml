# BOM: Payment Gateway
asset: "Payment Gateway"
os_version: "managed_service"
software_version: "Stripe API v2023-10-16 or Braintree SDK"
patch_level: current
known_cves: []
running_services:
  - HTTPS (outbound webhooks)
  - Payment processing API
detection_level: high
credentials_stored: false
notes: "Third-party managed payment gateway (Stripe or Braintree). PCI-DSS Level 1 certified by provider. API keys stored in backend server environment variables — see BackendServer BOM. Webhook signature verification should be enforced to prevent replay attacks. No self-hosted components; CVE exposure is managed by the payment provider."
