# BOM: APIGateway
asset: "APIGateway"
os_version: "managed_service"
software_version: "AWS API Gateway v2 (HTTP API) or Kong 3.4"
patch_level: current
known_cves: []
running_services:
  - HTTPS
  - OAuth2 token validation
detection_level: high
credentials_stored: false
notes: "Managed API gateway handling request routing, rate limiting, and OAuth token validation. AWS API Gateway or self-hosted Kong. WAF enabled with OWASP Core Rule Set. API keys rotated quarterly. Access logs forwarded to CloudWatch/SIEM. No known CVEs for managed service variant; Kong 3.x is patched to latest."
