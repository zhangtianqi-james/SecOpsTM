# BOM: BackendServices
asset: "BackendServices"
os_version: "ubuntu_22.04"
software_version: "Node.js 18 / gRPC microservices (sub-model: backend/model.md)"
patch_level: current
known_cves: []
running_services:
  - HTTP (internal)
  - gRPC
  - TCP (database)
detection_level: medium
credentials_stored: false
notes: "Backend microservices entry point with sub-model drill-down to backend/model.md. Contains APIGateway, AuthService, OrderService, and ProductDB. gRPC interfaces between services. Internal HTTP communication between WebApp and BackendServices not encrypted (plaintext on internal network). Sub-model reference enables full drill-down GDAF analysis into auth and order service internals."
