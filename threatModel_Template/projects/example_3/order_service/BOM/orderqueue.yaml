asset: "OrderQueue"
os_version: "Ubuntu 22.04 LTS"
software_version: "RabbitMQ 3.12.6"
patch_level: current
known_cves: []
running_services:
  - rabbitmq
  - rabbitmq-management
  - node-exporter
detection_level: medium
credentials_stored: true
notes: "RabbitMQ cluster (3 nodes). Durable queues with dead-letter exchange. Management UI restricted to management VLAN. Mutual TLS between nodes and between producers/consumers. Message TTL set to 24h. No PAN data in queue messages — only order references and payment intent IDs."
