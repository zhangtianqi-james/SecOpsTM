asset: "ShippingService_2"
os_version: "Ubuntu 22.04 LTS"
software_version: "Go 1.21 / chi 5.0.10"
patch_level: current
known_cves: []
running_services:
  - shipping-service
  - node-exporter
detection_level: medium
credentials_stored: true
notes: "Standby instance — identical to ShippingService_1. Same Kubernetes Secret exposure. Confirm both instances use separate API key rotation schedule or share the same key (risk: blast radius of key compromise doubles)."
