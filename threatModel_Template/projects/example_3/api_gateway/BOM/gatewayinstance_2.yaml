asset: "GatewayInstance_2"
os_version: "Ubuntu 22.04 LTS"
software_version: "Kong Gateway 3.4.2"
patch_level: outdated
known_cves:
  - CVE-2023-49107
running_services:
  - kong
  - kong-admin-api
  - node-exporter
detection_level: high
credentials_stored: true
notes: "Identical configuration to GatewayInstance_1. Active-active deployment. Same CVE exposure — both instances should be upgraded together. Admin API check required on both nodes."
