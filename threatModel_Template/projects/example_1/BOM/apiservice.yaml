# BOM: APIService
asset: "APIService"
os_version: "ubuntu_22.04"
software_version: "Kong Gateway 3.4 or AWS API Gateway v2"
patch_level: current
known_cves: []
running_services:
  - HTTPS
  - API routing
detection_level: medium
credentials_stored: false
notes: "API gateway entry point with sub-model drill-down to sub_project_A. Routes external traffic to NormalService. Authentication plugin configured but rate limiting not enforced. Sub-model reference enables GDAF graph traversal into internal database topology."
