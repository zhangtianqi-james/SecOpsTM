# BOM: Device Management Service
asset: "Device Management Service"
os_version: "managed_service"
software_version: "AWS IoT Device Management or custom portal v1.4"
patch_level: current
known_cves: []
running_services:
  - HTTPS
  - MQTT (OTA updates)
detection_level: medium
credentials_stored: true
notes: "Device management portal for firmware OTA updates, device provisioning, and remote command dispatch. Admin credentials stored in portal. Firmware update signing enforced via code signing certificate but revocation checking not always online. Bulk command dispatch capability could be abused to push malicious firmware to all registered gateways simultaneously."
