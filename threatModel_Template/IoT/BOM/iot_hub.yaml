# BOM: IoT Hub
asset: "IoT Hub"
os_version: "managed_service"
software_version: "AWS IoT Core or Azure IoT Hub (managed)"
patch_level: current
known_cves: []
running_services:
  - MQTTS
  - HTTPS
  - AMQP
detection_level: high
credentials_stored: false
notes: "Managed cloud IoT Hub service. Device certificates provisioned via cloud PKI. Message routing rules define how sensor data flows to storage and analytics. Device-to-cloud authentication uses X.509 certificates — certificate rotation schedule not formally defined. Cloud-to-device command channel exists and is used by Device Management Service."
